<?php
$username = "daniel prueba"; // asignamos el valor "daniel prueba" a la variable $username
echo $username; // mostramos el valor de la variable $username
echo "<br>"; // mostramos un salto de línea
$current_user = $username; // asignamos el valor de la variable $username a la variable $current_user
echo $current_user; // mostramos el valor de la variable $current_user
?>
